---
"@llm-workbench/runtime": patch
---

Add JSDoc documentation to WorkbenchSession's public API (session.ts) — every public method now has a description, `@param`/`@returns`, and `@throws` for the WorkbenchError codes it can actually raise.
